cloud: "do"
ingress:
  hostname: <your-hostname>
  nginx:
    enabled: true
cert-manager:
  enabled: true

# Use small PVC in CI
clickhouseOperator:
  storage: 1Gi
kafka:
  persistence:
    size: 1Gi
postgresql:
  persistence:
    size: 1Gi
redis:
  master:
    persistence:
      size: 1Gi
