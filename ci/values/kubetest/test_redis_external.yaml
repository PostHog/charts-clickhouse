cloud: local

redis:
  enabled: false

externalRedis:
  host: "redis-external.posthog.svc.cluster.local"
  port: 6379

#
# For the purpose of this test, let's disable service persistence
#
clickhouse:
  persistence:
    enabled: false
kafka:
  persistence:
    enabled: false
pgbouncer:
  persistence:
    enabled: false
postgresql:
  persistence:
    enabled: false
zookeeper:
  persistence:
    enabled: false
