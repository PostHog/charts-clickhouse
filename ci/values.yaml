cloud: k3s

ingress:
  nginx:
    enabled: true
    redirectToTLS: false
  letsencrypt: false

web:
  secureCookies: false

clickhouseOperator:
  storage: 1Gi

redis:
  usePassword: false
  password: ""
  master:
    persistence:
      size: 1Gi
